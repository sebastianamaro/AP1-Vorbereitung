Lösungen

2.7.8 OOP

Ausgangsszenario:

Die Entwicklungsabteilung der IT-Firma ConSystem GmbH soll eine Anwendung zur Darstellung von
geometrischen Grundformen (Kreis, Dreieck, ...) entwickeln. Dazu hat ein Mitarbeiter ein erstes
Konzept zum Aufbau der Klassen verfasst. Das Konzept ist programmiersprachenunabhängig in einer
Pseudocode-Variante geschrieben. Bei der Umsetzung des Konzeptes in die Sprache Java stellte ein
anderer Mitarbeiter fest, dass das Programm nicht korrekt arbeitet.

Aufgabenstellung:
Als erfahrene/-r Auszubildende/-r der Firma erhalten Sie den Auftrag, den Code zu analysieren und auf
mögliche Fehler zu prüfen. Achten Sie dabei besonders auf das Konzept des Polymorphismus.

A

Aufbau einer Klassenhierarchie ConSystem
GmbH
Klasse Basisform Fehler 2:
( Methode ZeichneAufBildschirm sollte als
virtuell gekennzeichnet werden.
privat:
Bezeichnung: STRING Anwendung:
Öffentlich:

Fehler 1: Basisform Elemente [1..3]

Konstruktor(STRING)
Standardkon- Destruktor() POINT P1(1,2), P2(4,7), P3(10,14)
struktor fehlt ZeichneAufBildschirm() Elemente[1] := Kreis("K1", P1, 10.5)
) Elemente[2] := Kreis("K2", P2, 3.75)

Elemente[3] := Dreieck("D1", P1, P2, P3)

Klasse Dreieck: erbt von Basisform N
FÜR i := 1 BIS 3 mit Schrittweite 1

\ Elemente[i].ZeichneAufBildschirm()
privat:
P1, P2, P3: POINT ENDE FÜR
Öffentlich:
Konstruktor()
Konstruktor(STRING, POINT, POINT, POINT)
Destruktor()
ZeichneAufBildschirm (POINT, POINT, POINT) Fehler 3: . En
Die Methode ZeichneAufBildschirm muss
) so deklariert werden, wie in der Basis-
Klasse Kreis: erbt von Basisform klasse, also ohne Parameter. Ansonsten
( ist die Methode nicht polymorph.
N Fehler 4:
VA Der Konstruktor muss auch noch
P: POINT Parameter vom Typ STRING und
R: DOUBLE DOUBLE haben.
Öffentlich:
cc Konstruktor() Fehler 5:
oO Konstruktor(POINT) siehe Fehler 3!
R Destruktor()
—J ZeichneAufBildschirm(POINT, DOUBLE)
:ö )
A

282
