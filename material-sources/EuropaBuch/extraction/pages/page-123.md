
---
## Page 123
---

Software


**[VISUAL: SOFTWARE DEVELOPMENT PROCESS HEADER]**
Header image introducing software development process concepts for a presentation.


## Ausgangsszenario:

Als Auszubildende/-r der IT-Firma ConSystem GmbH sollen Sie eine Prasentation vorbereiten, in der grundlegende Aspekte von Softwareentwicklungsprozessen dargestellt werden. 1hr Ausbilder hat einige lnformationen zusammengetragen, die von lhnen aufbereitet werden müssen.


## Aufgabenstellung:

Analysieren Sie die lnformationen und bereiten Sie die lnformationen korrekt für die Prasentation auf.


## 1 nformationen zum Softwareentwicklungsprozess

lnformation 1: Es soll der Prozess vom Quellcode zum ausführbaren Programm dargestellt werden. Bringen Sie die gegebenen Symbole in die korrekte Abfolge.

lhre Darstellung:

Linker

Ausführbares Biblio-

~


# íl

Gegebene Symbole:


# ij

Compiler Quellcode


# ~íl

Programm theken


**[VISUAL: SOURCE CODE TO EXECUTABLE PROCESS DIAGRAM]**
A diagram showing the compilation process with symbols to be arranged in correct order:

Given symbols (to be arranged):
```
[Quellcode] → [Compiler] → [Objektcode] → [Linker] ← [Bibliotheken]
                                              ↓
                                    [Ausführbares Programm]
```

Students arrange these symbols showing: Source code → Compiler → Object code → Linker (+ Libraries) → Executable program

**[VISUAL: JAVA CODE WITH OOP TERMINOLOGY LABELS]**
An annotated Java class example with arrows/callouts pointing to programming concepts:

```java
class GrundForm {                        ← [Klassendefinition]
    private String bezeichnung;          ← [Attribut] [Geschützter Zugriff]

    public GrundForm() {                 ← [Konstruktor] [Öffentlicher Zugriff]
    }

    GrundForm(String _bezeichnung) {     ← [Parameterkonstruktor]
        if (_bezeichnung != "") {        ← [Selektion]
            bezeichnung = _bezeichnung;   ← [Zuweisung]
        }
    }

    public String getBezeichnung() {     ← [Methode] [Datentyp: String]
        return bezeichnung;
    }
}

class Viereck extends GrundForm { }      ← [Vererbung]
```

Students label each marked section with the correct programming terminology.

lnformation 2: Anhand eines Java-Quellcode-Beispiels sollen wesentliche Bezeichnungen aus der Programmierung dargestellt werden. Beschriften Sie den Quellcode entsprechend.


### class GrundForm


# 0


# ]


# ~------~

{ priva~

e_______________ _______)


### Bezeichnungen:


**[VISUAL: SOURCE CODE TO EXECUTABLE PROCESS DIAGRAM]**
A diagram showing the compilation process with symbols to be arranged in correct order:

Given symbols (to be arranged):
```
[Quellcode] → [Compiler] → [Objektcode] → [Linker] ← [Bibliotheken]
                                              ↓
                                    [Ausführbares Programm]
```

Students arrange these symbols showing: Source code → Compiler → Object code → Linker (+ Libraries) → Executable program

**[VISUAL: JAVA CODE WITH OOP TERMINOLOGY LABELS]**
An annotated Java class example with arrows/callouts pointing to programming concepts:

```java
class GrundForm {                        ← [Klassendefinition]
    private String bezeichnung;          ← [Attribut] [Geschützter Zugriff]

    public GrundForm() {                 ← [Konstruktor] [Öffentlicher Zugriff]
    }

    GrundForm(String _bezeichnung) {     ← [Parameterkonstruktor]
        if (_bezeichnung != "") {        ← [Selektion]
            bezeichnung = _bezeichnung;   ← [Zuweisung]
        }
    }

    public String getBezeichnung() {     ← [Methode] [Datentyp: String]
        return bezeichnung;
    }
}

class Viereck extends GrundForm { }      ← [Vererbung]
```

Students label each marked section with the correct programming terminology.

'-./ public GrundForm() {

Óffentlicher Zugriff

Klassendefinition


# } bezeichnung ª ~ J

Vererbung

Attribut

Selektion


# ~-----------


## if (_bezeichnung != "" ) bezeichnung = _bezeichnung;

Gr un d Fo r m ( _bezeichnung) {

Parameterkonstruktor

Methode


# ~

} ~ public getBezeichnung() {

Geschützter Zugriff

return bezeichnung;


**[VISUAL: SOURCE CODE TO EXECUTABLE PROCESS DIAGRAM]**
A diagram showing the compilation process with symbols to be arranged in correct order:

Given symbols (to be arranged):
```
[Quellcode] → [Compiler] → [Objektcode] → [Linker] ← [Bibliotheken]
                                              ↓
                                    [Ausführbares Programm]
```

Students arrange these symbols showing: Source code → Compiler → Object code → Linker (+ Libraries) → Executable program

**[VISUAL: JAVA CODE WITH OOP TERMINOLOGY LABELS]**
An annotated Java class example with arrows/callouts pointing to programming concepts:

```java
class GrundForm {                        ← [Klassendefinition]
    private String bezeichnung;          ← [Attribut] [Geschützter Zugriff]

    public GrundForm() {                 ← [Konstruktor] [Öffentlicher Zugriff]
    }

    GrundForm(String _bezeichnung) {     ← [Parameterkonstruktor]
        if (_bezeichnung != "") {        ← [Selektion]
            bezeichnung = _bezeichnung;   ← [Zuweisung]
        }
    }

    public String getBezeichnung() {     ← [Methode] [Datentyp: String]
        return bezeichnung;
    }
}

class Viereck extends GrundForm { }      ← [Vererbung]
```

Students label each marked section with the correct programming terminology.

Datentyp

Zuweisung

Konstru ktor


# } / __


# ~


# )


# ~

class Viereck extends GrundForm { }

121
